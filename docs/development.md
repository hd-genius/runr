# Development

Unfortunately I haven't had much time to put into this document yet, so it currently serves as a ***VERY*** rough outline of what you need to know to work on the project. In it's current state, simply reading over `./.github/workflows/build.yml` and `./.github/workflows/quality-checks.yml` probably serve as a better guide for what's going on and how to do any thing. 

## Installing dependencies

1. install Python 3
2. install pipenv
3. install pipenv dependencies

## Running tests

`pipenv run test`

## Formatting

This has not yet been setup but it will in the future.

## Running without installing

`python -m runr my-script`